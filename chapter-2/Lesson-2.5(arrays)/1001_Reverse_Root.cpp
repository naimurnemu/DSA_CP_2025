#include <bits/stdc++.h>
using namespace std;

int main()
{
  ios:: sync_with_stdio(false);
  cin.tie(nullptr);
  
  long long int item;
  while (cin >> item) 
  {
    cout << fixed << setprecision(4) << sqrt(item) << endl;
  }
  
  
  return 0;
}