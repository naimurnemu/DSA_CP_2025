#include <bits/stdc++.h>
using namespace std;

int main()
{
  ios:: sync_with_stdio(false);
  cin.tie(nullptr);
  
  int r;
  cin >> r;

  cout << fixed << setprecision(2);
  cout << 2 * M_PI * r;
  
  return 0;
}